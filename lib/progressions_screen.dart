import 'package:flutter/material.dart';

class ProgressionsScreen extends StatelessWidget {

 @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('Progressions')
    );
  }
}