import 'package:flutter/material.dart';

class ChordBankScreen extends StatelessWidget {

 @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('Chord Bank')
    );
  }
}